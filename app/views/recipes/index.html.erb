<div class="col-md-8">
  <h1>
    All Recipes
  </h1>
  <%= render partial: 'search', locals: { recipe: @recipe}  %>

  <% @recipes.each do |recipe| %>
    <p><%= link_to recipe.title, recipe.category %></p>
  <% end %>
</div>
